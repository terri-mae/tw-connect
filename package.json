{
  "name": "tw-connect",
  "version": "1.0.0",
  "description": "TW Connect CRM — monorepo root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,UI -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "seed:admin": "npm run seed:admin --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
